# =============================================================================
# lowlighter/metrics — workflow de ejemplo (DESACTIVADO por defecto)
# =============================================================================
# Para activarlo:
#   1. Renombra este archivo a metrics.yml
#   2. Crea un Personal Access Token (classic) con permisos: read:user, repo
#   3. Añádelo como secret del repo con el nombre METRICS_TOKEN
#   4. Descomenta el bloque de abajo
#   5. NUNCA pegues el token directamente en este archivo ni en el README
# =============================================================================

# name: Metrics
#
# on:
#   schedule:
#     - cron: "0 0 * * *"   # una vez al día
#   workflow_dispatch:
#
# permissions:
#   contents: write
#
# jobs:
#   metrics:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: lowlighter/metrics@latest
#         with:
#           token: ${{ secrets.METRICS_TOKEN }}
#           user: Diego2005z
#           template: classic
#           config_timezone: America/Lima
#           base: header, activity, community, repositories
#           plugin_languages: yes
#           plugin_languages_analysis_timeout: 15
#           plugin_languages_categories: markup, programming
#           plugin_languages_colors: github
#           plugin_languages_limit: 8
#           plugin_languages_recent_load: 300
#           plugin_languages_sections: most-used
#           plugin_languages_threshold: 0%
#           filename: metrics.svg
